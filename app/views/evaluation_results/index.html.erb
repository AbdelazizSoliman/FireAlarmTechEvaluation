<h1>
  Evaluation Results for
  <%= @supplier.supplier_name %> /
  <%= @subsystem.name %>
</h1>

<div class="mb-4">
  <%= button_to "Re-Evaluate Now",
                evaluate_evaluation_results_path(
                  supplier_id:  @supplier.id,
                  subsystem_id: @subsystem.id
                ),
                method: :post,
                class: "btn btn-primary" %>

  <%= link_to "Download Excel",
              download_evaluation_results_path(
                supplier_id:  @supplier.id,
                subsystem_id: @subsystem.id
              ),
              class: "btn btn-secondary ml-2" %>
</div>

<table class="table-auto …">
  <thead>
    <tr class="bg-gray-200">
      <th>Attribute</th>
      <th>Submitted</th>
      <th>Standard</th>
      <th>Tolerance</th>
      <th>Degree</th>
      <th>Status</th>
      <th>Case</th>
      <th>Logic</th>
    </tr>
  </thead>
  <tbody>
    <% @results.each do |r| %>
      <tr class="<%= r.status == 'pass' ? 'bg-green-100' : 'bg-red-100' %>">
        <td><%= "#{r.table_name}.#{r.column_name}" %></td>
        <td><%= r.submitted_value %></td>
        <td><%= r.standard_value  %></td>
        <td><%= r.tolerance       %></td>
        <td><%= r.degree          %></td>
        <td><%= r.status.upcase   %></td>
        <td><%= r.combo_case      %></td>
        <td><%= r.combo_logic     %></td>
      </tr>
    <% end %>
  </tbody>
</table>

