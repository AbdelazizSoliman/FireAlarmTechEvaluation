<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Fire Alarm Control Panel Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    table, th, td {
      border: 1px solid black;
    }
    th, td {
      padding: 8px;
      text-align: left;
    }
  </style>
</head>
<body>
  <h1>Fire Alarm Control Panel Evaluation</h1>
  <p><strong>Notification:</strong> <%= @notification.title %></p>
  <p><strong>Submitted By:</strong> <%= @notification.body %></p>

  <h2>Fire Alarm Control Panel Details</h2>
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Value</th>
      </tr>
    </thead>
    <tbody>
      <% @fire_alarm_control_panel.attributes.each do |key, value| %>
        <tr>
          <td><%= key.humanize %></td>
          <td><%= value %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
