<% @project_scope.systems.each do |system| %>
  <div class="mb-4">
    <h3 class="text-md font-semibold"><%= system.name %></h3>
    <%= link_to "View #{system.name}", project_project_scope_system_path(@project_scope.project, @project_scope, system), class: "bg-blue-500 text-white py-2 px-4 rounded" %>
  </div>
<% end %>
