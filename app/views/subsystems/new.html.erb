<%= form_with model: [@project, @project_scope, @system, @subsystem] do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit 'Create Subsystem' %>
  </div>
<% end %>